import {ViewStyle} from 'react-native';

export const initialItemsNum = 20;

export const tabBarStyle: ViewStyle = {
  display: 'flex',
  position: 'absolute',
  bottom: 20,
  elevation: 1,
  borderRadius: 50,
  marginHorizontal: 80
};
